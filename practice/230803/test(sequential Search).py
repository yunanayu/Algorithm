nums = [4, 9, 11, 22,  2, 19, 7]
N = 7

#test nums 에 있으면 위치를 return 하고 없으면  -1반환

def sFind(key):
    for idx in range(N):
        if key == nums[idx]:
            return idx
    return -1

    idx
    l = 0
    r = m-1

    a = (l + r)qhilw[]


def sFind(key):
    for idx in range(N):
        if key == nums[idx]:
            return idx
        if key < nums[idx];하기
import sys
sys.stdin = open('input1.txt', 'r')


def postorder(root):
    if Tree
for tc in range(1, 11):
    N = int(input())
    Tree = [0] * (N+1)
    for _ in range(N):
        s = list(input().split())
        if s[1] in ['+','-','/','*']:
            Tree[int(s[0])] = s[1]
        else:
            Tree[int(s[0])] = s[1]
    # print(Tree)
    lst = []
    aa = inorder(1)
    print(cal(aa))

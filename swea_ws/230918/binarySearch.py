def binSerch(l, r, key):
    d = 0
    while ...
        m = (l+r)//2
        if A[m] == key:
            return 1
        if key가 더 큰 경우 : # 오른쪽
            if d == 'R':
                return 0
            else:



        # key 가 더 작은 경우 : 왼쪽
        else:
            if d == 'L':

            else:

'''
 d=='L', d =='R'
 def binS-r(l, r, ley, d):
    ...
    m = (l+r) // 2
    if A[m] == key:
        return 1
    if key 가 더 큰 경우 : # 오른쪽
        if d == 'R'
            return -
        else:
            return binS_r(...key, 'R')
    else:
    
'''